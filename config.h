/* yuxtapa configuration, for YOU to edit.
 * These configurations only affect the server; if you are only
 * building the client, leave this file as it is and run "make client".
 * If you want to build the server, please read on. */
#ifndef YUX_CONFIG_H
#define YUX_CONFIG_H

// For people completely lost with C++ programming, to "uncomment" means
// to delete the two '/' symbols from the beginning of the line.

// Uncomment to enable printing debug info (affects the server mainly):
#define DEBUG

// Uncomment to build a maptest server that only generates maps:
//#define MAPTEST

#endif
